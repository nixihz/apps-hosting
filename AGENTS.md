
## 文件大小和函数粒度

以下是硬性倾向，不是等到完全超限才处理：

- 新增源文件优先控制在 250 行以内。
- 超过 300 行的文件继续新增逻辑前，先拆分职责。
- 单个函数优先控制在 40 行以内。
- 超过 60 行的函数必须优先拆成命名清晰的辅助函数。
- 渲染函数、请求处理函数、worker 主流程函数不得混合数据读取、校验、状态变更、HTML 拼接和副作用；至少按职责拆开。
- 大段模板字符串、长条件分支、重复 HTML 片段、重复 fetch/response 逻辑都应抽成小函数。

